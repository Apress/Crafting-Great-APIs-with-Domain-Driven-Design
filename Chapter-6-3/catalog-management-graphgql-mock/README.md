# GraphQL Mock Server

## Requirements

* Node >=22.0.0
* yarn

## Run

Install & start it:
```sh
yarn install
yarn start
```
then go to [http://localhost:4000/graphql](http://localhost:4000/graphql).

